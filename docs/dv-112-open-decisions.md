# DV-112 — 팀 결정이 필요한 안건

사업자등록증 검증(DV-112)을 구현하면서, **dev의 설계 문서(`team-creation-api-spec.md`)에 없던 것들이
코드에 들어갔다.** 설계 문서를 정본으로 삼기로 한 이상, 이것들은 "구현했으니 끝"이 아니라 팀이
스펙으로 받아들일지 결정해야 하는 항목이다.

이 문서는 그 목록이다. 각 항목마다 **무엇이 들어갔고 / 왜 들어갔고 / 무엇을 잃는지**를 적었다.
결정이 나면 해당 내용을 설계 문서에 반영하고 이 문서에서 지운다.

> 코드는 지금 그대로 두었다. 결정 전에 지우지도, 정당화하지도 않는다.

---

## 1. 광고업 심사 신설 — 가장 큰 안건

> ✅ **결정됨 (2026-07 팀 피드백).** 광고업 심사·자동 판정을 **도입하지 않는다.** MVP는 OCR
> 결과를 사용자가 확인하면 그대로 팀을 생성한다. 국세청 진위확인을 포함한 검증 전체가 구현
> PR과 함께 보류됐다 — `server-verification-spec.md`는 기능을 다시 살릴 때를 위한 설계 기록으로
> 남긴다. 아래는 결정 당시의 안건 원문이다.

### 무엇이 들어갔나

**"광고업 사업자만 팀을 만들 수 있다"**는 규칙이 생겼다. 업태·종목을 OCR로 읽어 광고 관련 키워드와
대조하고, 광고업이 아니라고 판정되면 **팀 생성을 거부한다**(`400 BUSINESS_400_105~107`).

관련 산출물: `AdvertisingClassifier`, 판정 규칙 5~7번, `business_item` 컬럼 마이그레이션
(dev의 `V6`·`V7`과 충돌을 피해 `V8`로 리네이밍됨), `docs/server-verification-spec.md`(270줄),
업로드 토큰 payload 확장, OCR Lambda 계약 변경.

### 왜 안건인가

dev 설계 문서는 정반대를 명시했다.

> OCR도 이 중 문서에 실제로 인쇄돼 있는 4개(사업자명/대표자명/사업자등록번호/개업일자)만 추출하면 된다.
> **업태·사업장 소재지는 이번 화면에 아예 없으므로 OCR 대상이 아니다.**

즉 광고업 심사는 **설계에 없던 제품 요구사항**이다. 구현 과정에서 생긴 것이지 합의된 것이 아니다.

### 무엇을 잃는가 (이게 핵심)

업태·종목은 **화면에 입력란이 없다.** 사용자가 고칠 수 있게 하면 음식점업 사업자가 "광고대행"으로
바꿔 스스로를 승인시킬 수 있어서, 일부러 폼에서 빼고 OCR 원본을 서명 토큰으로만 나르기 때문이다.

그 대가로 **OCR이 업태·종목을 잘못 읽으면 사용자에게 탈출구가 없다.**

- 정상적인 광고대행사인데 종목이 "마케팅"으로만 잡히면 → `400`, 팀 생성 불가
- OCR이 업태·종목 칸을 못 읽으면 → `400`, 팀 생성 불가
- 사용자가 할 수 있는 건 **재업로드뿐**이고, 재업로드는 24시간 10회 한도를 소모한다

보안(위조 방지)과 가입 성공률을 맞바꾼 설계다. **기획·프론트와 합의가 필요하다.**

### 결정할 것

- [x] 광고업 심사를 제품 요구사항으로 확정할 것인가? → **확정하지 않는다.** 검증
      전체(국세청 진위확인·광고업 분류·자동 판정)를 MVP에서 제외하고 관련 PR을 보류했다.
- [x] OCR 오독 시 사용자 구제 경로 → 판정 자체가 없어졌으므로 **안건 소멸.** OCR이 무엇을
      읽든(못 읽든) 사용자가 확인하면 팀은 만들어진다.
- [x] 되돌리는 범위 → 판정·검증 코드는 들어냈지만 **OCR은 4필드가 아니라 6필드를 유지한다** —
      업태·종목이 화면 표시·저장용으로 남았기 때문이다 (2번 참고).

---

## 2. OCR Lambda 계약 변경 — 배포 순서 주의 (크로스 레포)

> ✅ **결정됨.** 6필드 응답 계약(`+businessType`, `+businessItem`)을 **유지한다.** 다만 용도가
> 바뀌었다 — 광고업 분류의 입력이 아니라 **화면 표시·DB 저장용**이다. 못 읽으면 `null`로
> 저장될 뿐 팀 생성이 막히지 않으므로, 아래의 "모든 팀 생성이 400으로 실패" 위험도 사라졌다.

Lambda는 별도 레포(`biz-eligibility`)에서 배포한다. **계약이 바뀌었다.**

| | dev 설계 | 현재 구현 |
|---|---|---|
| 요청 | `{ bucket, key }` | `{ action: "ocr", bucket, key }` |
| 응답 | 4필드 | **6필드** (`+businessType`, `+businessItem`) |

**Lambda가 백엔드보다 먼저 배포되지 않으면 조용히 망가진다.** 옛 Lambda가 업태·종목을 안 돌려주면
분류가 `unknown`이 되고 → 판정 규칙 5번 → **모든 팀 생성이 `400`으로 실패한다.** OCR 실패는 업로드
단계에서는 흡수되지만 팀 생성 단계에서는 치명적이다.

광고업 심사는 접었지만(1번) **Lambda 계약은 6필드로 유지한다** — 업태·종목이 화면 표시·저장에
쓰이기 때문이다. 옛 Lambda(4필드)가 남아 있어도 이제 팀 생성이 실패하지는 않고, 업태·종목이
`NULL`로 저장될 뿐이다.

### 결정할 것

- [x] Lambda 6필드 계약 유지로 확정. 배포 순서 리스크는 "모든 팀 생성 실패"에서 "업태·종목
      `NULL` 저장"으로 낮아졌다 — 그래도 Lambda 레포를 백엔드보다 먼저 배포한다.

---

## 3. 설계 문서에 없는 API·응답 코드

전부 dev 설계 문서에 **한 번도 등장하지 않는다.** 구현하면서 필요해서 넣은 것들이다.

| 항목 | 코드 | 왜 넣었나 | 빼면 무슨 일이 생기나 |
|---|---|---|---|
| 업로드 횟수 제한 | `429 BUSINESS_429_001` (24h / 10회) | 업로드 1회 = OCR 1회 = 과금. 인증만으로 무제한 호출 가능했다 | 로그인한 사용자 하나가 OCR 비용을 계속 태울 수 있다 |
| 사용자당 팀 1개 강제 | `409 BUSINESS_409_002` | `UserMeResponse`가 `teamId`를 하나만 반환한다 | 두 번째 팀이 만들어지되 **사용자가 접근할 수 없는 유령 팀**이 된다 |
| 사업자등록증은 OWNER만 | (전용 코드 없음 — 정책) | ERD 4절 "권한 기준"에 근거 | 일반 멤버가 팀의 사업자 정보를 다룰 수 있다 |

앞의 둘은 코드에 들어간 방어다. 셋째(OWNER만)는 **재제출 드롭으로 강제하던 코드가 사라져
지금은 정책 서술만 남았다** — 현행 플로우에서는 업로드가 인증만 확인하고 팀 생성자가 자동
OWNER라, 일반 멤버가 사업자 정보를 다루는 경로 자체가 없다. 재제출·수정 기능이 생기면 그때
강제 코드와 함께 되살린다.
앞의 둘은 "없으면 실제 사고가 나는" 방어라서, 저는 **문서에 추가해 스펙으로 승격**하는 쪽을 권한다.
다만 설계 문서를 정본으로 삼기로 한 원칙의 예외이므로 팀 확인을 받는다.

(재제출 API와 그에 딸린 코드 — 승인 후 재제출 차단 `409 BUSINESS_409_001`, OWNER 검사
`403 BUSINESS_403_002` — 는 재제출 자체가 드롭되면서 이 표에서 빠졌다. 반려 개념이 없어
재제출도 없다. 공개됐던 번호는 결번으로 유지한다.)

### 결정할 것

- [ ] 위 3개를 스펙으로 승격하고 설계 문서에 반영할 것인가? (권장)
- [ ] 승격하지 않는다면 어느 것을 코드에서 뺄 것인가?

### 알려진 한계 (승격한다면 함께 결정)

업로드 횟수 제한은 **인스턴스 메모리 기반**이라 스케일 아웃하면 서버 대수 × 10회가 된다.
지금은 EC2 1대라 문제없지만, 늘리려면 Redis 등 공유 저장소가 필요하다.

---

## 4. 국세청 API 실패: `500` vs `502`

> ✅ **해소됨.** 국세청 진위확인 자체가 MVP에서 제외되면서(1번 참고) 이 안건은 소멸했다.
> 기능을 다시 살릴 때 그때의 프론트 5xx 처리 방침과 함께 재논의한다.

**당시 상태는 문서대로 `500`(`BUSINESS_500_004`)이었다.** 구현이 한때 `502`였으나 문서를 정본으로 삼아 되돌렸다.

의미상으로는 `502 Bad Gateway`가 더 정확하다 — 우리 서버 결함이 아니라 상위 의존 서비스(국세청)의
실패이기 때문이다. 프론트가 5xx를 어떻게 다루는지(자동 재시도 여부)에 따라 실익이 갈린다.

### 결정할 것

- [x] `502`로 개정할 것인가? → **안건 소멸** — 국세청 연동 자체가 MVP에서 빠졌다.

---

## 5. "팀 없음"을 404로 할까 403으로 할까 — 도메인 내 정책 충돌

같은 `team` 도메인 안에서 **정반대 정책이 공존한다.**

| | 백엔드 A (초대코드·팀원 관리, DV-113/120) | 백엔드 B (사업자등록증, DV-112) |
|---|---|---|
| 팀이 없을 때 | `404 TEAM_404_001` | `403` (404와 구분하지 않음) |
| 근거 | 설계 문서 6절이 404를 명시 | 팀 존재 여부가 외부로 새는 것을 막으려고 의도적으로 통일 |

DV-112 쪽 주석에는 이렇게 적혀 있다 — *"구분해서 404/403으로 나눠 응답하면 팀의 존재 여부가 외부에
새어 나간다."* 그런데 백엔드 A는 바로 그 404를 쓴다. 프론트 입장에서도 같은 도메인의 API가 서로 다르게
동작하는 셈이다.

(에러 코드 접두사는 `TEAM_`/`BUSINESS_`로 갈려 있어 충돌은 없다.)

### 결정할 것

- [ ] 팀 도메인 전체를 어느 쪽으로 통일할 것인가?

---

## 6. `home-dashboard-api-spec.md`가 삭제됐다

`origin/dev`의 커밋 `fd87146 [DV-113] feat: 팀원 페이지 API 제작`에서 **1,530줄짜리 대시보드 설계
문서가 통째로 삭제됐다.** 팀원 API를 만드는 커밋이 대시보드 문서를 지운 것이라 **실수로 보인다.**

대시보드 API 9개는 코드에 멀쩡히 살아 있는데 스펙만 사라진 상태다.

### 결정할 것

- [ ] 삭제가 의도된 것인가? 아니라면 복구한다.
- [ ] 복구한다면 알려진 오류도 함께 고친다:
      - 존재하지 않는 컬럼명 4개 참조 (`dwell_1_2s_count` 등 → 실제는 `dwell_1_to_under_2s`)
      - `mediaUnitId`를 "항상 있다"고 기술 (ERD·SQL은 nullable) — 아래 7번과 연결

---

## 7. `campaigns.media_unit_id` — 문서끼리 모순 + 500 위험

| 출처 | 내용 |
|---|---|
| `mvp-database-erd.md` | "선택 전에는 NULL" (nullable) |
| `V1__init_schema.sql` | `media_unit_id BIGINT UNSIGNED **NULL**` |
| `home-dashboard-api-spec.md` (삭제됨) | "매체 선택이 필수라 **항상 값이 있다**" |

그리고 `DashboardCampaignDetailResponse`가 `campaign.getMediaUnit().getId()`를 **null 체크 없이**
역참조한다. `media_unit_id`가 NULL인 캠페인 행이 하나라도 생기면 상세조회가 **500**으로 죽는다.

DV-112 범위 밖이라 손대지 않았다. 별도 이슈로 다루는 편이 낫다.

### 결정할 것

- [ ] 컬럼을 `NOT NULL`로 좁힐 것인가, 응답을 nullable로 열고 코드에 null 가드를 넣을 것인가?

---

## 7-1. 팀 생성과 팀 합류가 교차하면 "사용자당 팀 하나"가 뚫릴 수 있다

팀 생성(DV-112)은 users 행을 잠그고 "이미 ACTIVE 팀이 있는가"를 재확인해 **생성끼리의**
동시 요청은 직렬화한다. 그런데 팀 합류(`TeamJoinService`, DV-120)는 teams 행만 잠그고
**요청자가 이미 다른 팀에 속해 있는지 검사하지 않는다.** 그래서 생성과 합류가 교차하면
(동시든 순차든) 한 사용자가 ACTIVE 팀 두 개에 속할 수 있다 — `UserMeResponse`가 teamId를
하나만 반환하는 가정이 깨진다.

합류 쪽 코드는 DV-112 범위 밖이라 손대지 않았다.

### 결정할 것

- [ ] 합류 API에도 "이미 소속된 팀이 있으면 409" 검사(가능하면 users 행 잠금과 함께)를 넣을 것인가? (권장)

---

## 8. 백엔드 A에 넘길 버그 2건 (DV-113 산출물)

`LocalDashboardMockDataInitializer` 관련이다. DV-112 쪽에서 우회했지만 근본 원인은 그쪽에 있다.

1. **주석과 코드가 다르다.** `application-local.yml` 주석은 "기본값은 꺼둔다"인데 실제 기본값은
   `true`(`LOCAL_DASHBOARD_MOCK_DATA_ENABLED:true`)다.
2. **`Clock`을 목킹하는 모든 테스트의 컨텍스트를 깨뜨린다.** 이 초기화기가 `ApplicationRunner`라
   컨텍스트 기동 시점에 `LocalDateTime.now(clock)`을 호출하는데, 시간을 고정하려고 `Clock`을
   `@MockitoBean`으로 바꾼 테스트에서는 `getZone()`이 `null`이라 NPE로 컨텍스트가 뜨지 못한다.
   (DV-112는 해당 테스트에서 초기화기를 끄는 것으로 우회했다.)
